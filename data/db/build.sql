CREATE TABLE IF NOT EXISTS assignments(
    UserID integer Default 0,
    Name text Default "No Name",
    Subject text Default "No Subject",
    DueDate text Default "No Due Date",
    Completed text Default "No"
);